# Test-only dependencies. Not required to run the application -- see
# requirements.txt for that. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==9.1.1
httpx==0.28.1
